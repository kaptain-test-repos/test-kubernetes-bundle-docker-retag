# Test Kubernetes Bundle Docker Retag

E2E test repo for `kubernetes-bundle-docker-retag` workflow in buildon-github-actions.

Tests retagging an upstream image (nginx:1.27 from Docker Hub) bundled together with hand-authored Kubernetes resources, lower-kebab name style with ALL name validation, preserve-all trailing newline mode, and custom config-sub-path.
