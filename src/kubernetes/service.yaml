apiVersion: v1
kind: Service
metadata:
  name: test-kube-bundle-docker-retag
spec:
  selector:
    app: test-kube-bundle-docker-retag
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
